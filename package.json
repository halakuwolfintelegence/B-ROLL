{
  "name": "cyber-arbab-video",
  "version": "3.2.0",
  "description": "AI Script-to-Video Engine with Admin Panel",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name cyber-arbab-video",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
